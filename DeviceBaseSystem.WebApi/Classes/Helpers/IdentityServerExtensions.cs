//using System.Linq;
//using System.Security.Claims;
//using System.Security.Principal;

namespace DeviceBaseSystem.WebApi.Classes.Helpers
{
    //public static class IdentityServerExtensions
    //{
    //    public static string GetISUserId(this IPrincipal User)
    //    {
    //        if (User == null)
    //            return string.Empty;

    //        return ClaimsPrincipal.Current.Claims.Where(c => c.Type == "sub").Select(s => s.Value).FirstOrDefault();
    //    }

    //    public static string GetClaim(this IPrincipal User, string claim)
    //    {
    //        return ClaimsPrincipal.Current.Claims.Where(c => c.Type == claim).Select(s => s.Value).FirstOrDefault();
    //    }
    //}
}